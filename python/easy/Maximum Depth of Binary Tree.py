1 вариант
class Solution:
    def maxDepth(self, root: Optional[TreeNode]) -> int:
        if not root:
            return 0
        return 1 + max(self.maxDepth(root.left), self.maxDepth(root.right))
#-------------------------------------------------------------------------------------
2 Вариант
class Solution:
    def maxDepth(self, root: Optional[TreeNode]) -> int:
        # Базовый случай: если дерево пустое, глубина равна 0
        if not root:
            return 0 
        
        # Счетчик уровней (глубины) дерева
        level = 0
        
        # Создаем очередь для BFS, изначально добавляем корневой узел
        q = deque([root])
        
        # Продолжаем обход, пока в очереди есть узлы
        while q:
            # Обрабатываем все узлы текущего уровня за одну итерацию
            # len(q) фиксирует количество узлов на текущем уровне
            for i in range(len(q)):
                # Извлекаем узел из начала очереди (FIFO)
                node = q.popleft()
                
                # Если у узла есть левый потомок, добавляем его в очередь
                # для обработки на следующем уровне
                if node.left:
                    q.append(node.left)
                
                # Если у узла есть правый потомок, добавляем его в очередь
                # для обработки на следующем уровне
                if node.right:
                    q.append(node.right)
            
            # После обработки всех узлов текущего уровня
            # увеличиваем счетчик глубины
            level += 1
        
        # Возвращаем максимальную глубину (количество уровней)
        return level
#-------------------------------------------------------------------------------------